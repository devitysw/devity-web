---
const { title, desc } = Astro.props;
---

<div class="flex flex-col space-y-2">
    <h5 class="text-lime font-semibold uppercase">{title ?? "title"}</h5>
    <div class="text-slate-400">
        {desc ?? "desc"}
    </div>
</div>
