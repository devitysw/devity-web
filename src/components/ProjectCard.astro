---
const { title, desc, moreInfo, image, color, btn } = Astro.props;
---

<div
    class="bg-[#161616] w-full rounded-3xl py-8 px-12 flex flex-col-reverse lg:flex-row space-x-4 justify-between items-stretch"
>
    <div class="flex flex-col h-full justify-between w-1/3 self-center">
        <h3 class=`font-bold ${color}`>{title}</h3>
        <p class="text-white opacity-65 mt-4 font-light tracking-wider">
            {desc}
        </p>
        <button class=`w-1/3 mt-12 ${btn}`>Viac</button>
    </div>
    <div class="w-1/2">
        <img
            src={image}
            alt="illustration"
            class="w-[90%] s:w-1/2 m:w-1/3 lg:w-2/3 mx-auto"
        />
    </div>
</div>
