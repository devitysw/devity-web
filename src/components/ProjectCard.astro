---
const { title, desc, moreInfo, image, color, btn } = Astro.props;
---

<div
    class="bg-[#161616] w-full rounded-3xl py-8 px-4 s:px-8 m:px-12 flex flex-col-reverse lg:flex-row space-x-4 justify-between items-stretch"
>
    <div
        class="flex flex-col h-full justify-between w-full lg:w-1/2 self-center mt-12 lg:mt-0"
    >
        <h2 class=`font-bold text-center lg:text-left ${color}`>{title}</h2>
        <p
            class="text-white opacity-65 mt-4 font-light tracking-wider text-center lg:text-left text-xs m:text-sm lg:text-base"
        >
            {desc}
        </p>
        <a
            href={moreInfo}
            target="_blank"
            class=`w-[90%] mx-auto text-center lg:ml-0 lg:w-1/3 mt-12 ${btn}`
        >
            Viac
        </a>
    </div>
    <div class="w-[90%] s:w-3/4 lg:w-1/2 self-center">
        <img
            src={image}
            alt="illustration"
            class="w-full xl:w-2/3 mx-auto self-center"
        />
    </div>
</div>
